#!/bin/sh
# Runs the shared safety + security gate before every push (terminal or tool).
# Enabled via `git config core.hooksPath .githooks` (set automatically by `npm install`).
exec sh .claude/hooks/precommit-safety.sh push
