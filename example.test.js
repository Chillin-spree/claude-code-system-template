// Placeholder test — replace with real tests for your app. Proves `node --test` runs.
import { test } from "node:test";
import assert from "node:assert/strict";

test("example placeholder", () => {
  assert.equal(1 + 1, 2);
});
